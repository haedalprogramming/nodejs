
/* ======================================================
   ⚡ Week 04 Study File: JavaScript DOM & 인터랙션 정리
   ====================================================== */

/* ------------------------------------------------------
   [1] JS 연결 위치 (매우 중요)
   - 반드시 </body> 닫기 직전에 연결
   - HTML이 먼저 로딩된 후 JS 실행
---------------------------------------------------------

<script src="script.js"></script>

------------------------------------------------------ */


/* ------------------------------------------------------
   [2] DOM 선택 기본 공식
   Select → Listen → Act
---------------------------------------------------------

// 1️⃣ Select (선택)
const title = document.querySelector('h1');

// 2️⃣ Act (행동)
title.innerText = "자바스크립트로 변경된 제목";
title.style.color = "red";

------------------------------------------------------ */


/* ======================================================
   🌙 [3] Dark Mode 구현
   ====================================================== */

/* -------------------------------
   1️⃣ Select
---------------------------------- */

const modeBtn = document.getElementById('mode-btn');
const body = document.querySelector('body');


/* -------------------------------
   2️⃣ Listen + 3️⃣ Act
   (주의: style.css에 .dark-mode 스타일이 정의되어 있어야 색이 변함!)
---------------------------------- */

modeBtn.addEventListener('click', function() {

    // dark-mode 클래스 토글
    body.classList.toggle('dark-mode');

    // 버튼 텍스트 변경
    if (body.classList.contains('dark-mode')) {
        modeBtn.innerText = "Light Mode";
    } else {
        modeBtn.innerText = "Dark Mode";
    }
});


/* ======================================================
   🧭 [4] Scroll Event - Navbar 변신
   ====================================================== */

/* ⚠️ 사전 준비
   - <nav> 태그에서 bg-dark 제거 (기본은 투명)
   - CSS에 transition 추가
   - style.css에 transition 추가 필수!
   - .navbar { transition: all 0.5s ease; }  ← 이거 없으면 뚝뚝 끊김
*/
const navbar = document.querySelector('.navbar');

window.addEventListener('scroll', function() {

    // ⚠️ 수업 후 반드시 제거 (성능 저하 원인)
    // console.log(window.scrollY);

    if (window.scrollY > 50) {
        navbar.classList.add('bg-dark');
        navbar.classList.add('shadow');
    } else {
        navbar.classList.remove('bg-dark');
        navbar.classList.remove('shadow');
    }
});


/* ======================================================
   🛑 [5] 자주 발생하는 에러
   ====================================================== */

/*
❌ Cannot read property of null

원인:
- getElementById('mode-btn') ← HTML에 id 오타

해결:
- HTML id와 JS id가 완전히 동일한지 확인
- 복사 & 붙여넣기 사용
*/


/* ======================================================
   🎯 핵심 요약
   ====================================================== */

/*
1. Select → Listen → Act 패턴을 기억하라
2. classList.toggle()은 다크모드 필수 치트키
3. scroll 이벤트는 매우 많이 실행됨 (console.log 제거)
4. 변수는 위, 이벤트는 아래 정리
*/


