# ⚡ Week 04. 숨 불어넣기 -- 동적 기능 (JavaScript DOM)

> "HTML/CSS가 마네킹이라면, 자바스크립트는 로봇의 엔진입니다." 이제
> 버튼을 누르면 화면이 바뀌고, 스크롤을 내리면 메뉴가 변하는 '진짜
> 웹사이트'를 만듭니다.

------------------------------------------------------------------------

## 🎯 이번 주 목표

-   **DOM(문서 객체 모델)**의 개념을 이해하고 자바스크립트로 HTML을
    조작한다
-   Event Listener를 사용하여 클릭, 스크롤 등의 사용자 행동을 감지한다
-   classList Toggle을 활용해 다크모드와 투명 내비게이션을 구현한다

**Key Tech:** JavaScript(ES6), DOM API, Event Listener, classList\
**Result:** 클릭하면 색이 반전되는 다크모드 + 스크롤 시 변하는 투명
내비게이션

------------------------------------------------------------------------

# 📅 Monday (오후 2.5h)

## 1️⃣ Setup: 엔진 장착하기 (30분)

HTML과 JS를 연결합니다. 위치가 매우 중요합니다.

``` html
<script src="https://cdn.jsdelivr.net/npm/bootstrap..."></script>
<script src="script.js"></script>
</body>
```

테스트:

``` javascript
alert('Hello Somago!');
```

------------------------------------------------------------------------

## 2️⃣ Core Concept: DOM과 선택자 (40분)

``` javascript
const title = document.querySelector('h1');
title.innerText = "자바스크립트로 바꾼 제목";
title.style.color = "red";
```

공식:

    document.querySelector('CSS선택자')

------------------------------------------------------------------------

## 3️⃣ Hands-on: 다크모드 스위치 만들기 (1시간 20분)

------------------------------------------------------------------------

### ✅ Step 1: HTML 버튼 추가

``` html
<button id="mode-btn" class="btn btn-outline-secondary">
  Dark Mode
</button>
```

------------------------------------------------------------------------

### ✅ Step 2: CSS 다크모드 정의 (🔥 중요 보강)

⚠️ **Critical Check: 부트스트랩과 다크모드 충돌 방지**

부트스트랩 기본 글자색이 검정이기 때문에,\
배경만 검게 바꾸면 글씨가 사라지는 문제가 발생합니다.

따라서 모든 글자색을 강제로 흰색으로 지정합니다.

``` css
/* 다크모드 기본 배경 + 글자색 */
.dark-mode {
    background-color: #000000;
    color: #ffffff;
}

/* 부트스트랩 기본 스타일 덮어쓰기 (중요!) */
.dark-mode a,
.dark-mode h1,
.dark-mode h2,
.dark-mode p {
    color: #ffffff !important;
}

/* 카드 색상 변경 */
.dark-mode .card {
    background-color: #333;
    color: white;
}
```

------------------------------------------------------------------------

### ✅ Step 3: JS로 토글 구현

``` javascript
const modeBtn = document.getElementById('mode-btn');
const body = document.querySelector('body');

modeBtn.addEventListener('click', function() {

    body.classList.toggle('dark-mode');

    if (body.classList.contains('dark-mode')) {
        modeBtn.innerText = "Light Mode";
    } else {
        modeBtn.innerText = "Dark Mode";
    }
});
```

------------------------------------------------------------------------

# 📅 Tuesday (오전 2.5h)

## 1️⃣ 스크롤 이벤트 (Navbar 변신)

⚠️ **Critical Check: Navbar 사전 정리**

스크롤 효과를 주기 전에\
`<nav class="navbar bg-dark">` 에서 `bg-dark` 또는\
`bg-body-tertiary` 클래스를 제거하여 **기본 상태를 투명하게** 만드세요.

------------------------------------------------------------------------

### ✅ CSS 보강 (부드러운 전환 효과)

``` css
.navbar {
    transition: all 0.5s ease;
}
```

------------------------------------------------------------------------

### ✅ JS 구현

``` javascript
const navbar = document.querySelector('.navbar');

window.addEventListener('scroll', function() {

    // console.log(window.scrollY); ← 수업 후 반드시 제거!

    if (window.scrollY > 50) {
        navbar.classList.add('bg-dark');
        navbar.classList.add('shadow');
    } else {
        navbar.classList.remove('bg-dark');
        navbar.classList.remove('shadow');
    }
});
```

------------------------------------------------------------------------

## 👨‍🏫 Teacher's Tip (성능 지도 포인트)

`console.log(window.scrollY);` 는\
스크롤할 때마다 1초에 수백 번 실행됩니다.

✔ 확인이 끝났으면 반드시 **주석 처리하거나 삭제**하세요.\
✔ 불필요한 로그는 성능을 크게 저하시킵니다.

------------------------------------------------------------------------

## 2️⃣ Refactoring

-   변수 선언은 위에
-   이벤트 리스너는 아래
-   주석은 미래의 나를 위해

------------------------------------------------------------------------

## 3️⃣ Final Deploy

-   Git Commit & Push
-   Vercel 자동 배포 확인
-   Show & Tell

------------------------------------------------------------------------

# 📝 과제

## 🎯 미션

나만의 인터랙션 포인트 하나 추가하기

예시: - 맨 위로 가기 버튼 - 이미지 클릭 확대 (Lightbox) - Scroll Reveal
애니메이션

## 📌 제출

최종 Vercel 링크 제출

