
// ======================================================
// 🎨 Week 02 Study File
// CSS Magic - 넷플릭스 스타일 UI 정리 (설명용 주석 포함)
// ======================================================


// ======================================================
// 1️⃣ CSS 파일 연결 (HTML에서 작성)
// ======================================================

/*
<link rel="stylesheet" href="style.css">

→ HTML과 CSS를 연결하는 코드
→ href 경로가 틀리면 CSS가 적용되지 않음
*/


// ======================================================
// 2️⃣ Reset CSS (브라우저 기본값 제거)
// ======================================================

/*
* {
    margin: 0;              // 모든 요소의 기본 바깥 여백 제거
    padding: 0;             // 모든 요소의 기본 안쪽 여백 제거
    box-sizing: border-box; // padding + border 포함해서 크기 계산 (필수)
}

body {
    background-color: #141414;              // 다크 모드 배경색
    color: white;                           // 기본 글자색
    font-family: 'Noto Sans KR', sans-serif; // 전체 폰트 설정
}
*/

// box-sizing: border-box를 사용하지 않으면
// width 계산이 꼬이기 때문에 반드시 설정


// ======================================================
// 3️⃣ Flexbox - 프로필 중앙 정렬
// ======================================================

/*
header {
    display: flex;            // 플렉스 박스 선언
    flex-direction: column;   // 세로 방향 정렬
    align-items: center;      // 가로축 가운데 정렬
    justify-content: center;  // 세로축 가운데 정렬
    padding: 3rem 0;          // 위아래 여백
    text-align: center;
}
*/

// align-items  : 가로 정렬
// justify-content : 세로 정렬


// ======================================================
// 4️⃣ 프로필 이미지 원형 만들기
// ======================================================

/*
header img {
    width: 120px;                 // 가로 크기
    height: 120px;                // 세로 크기
    border-radius: 50%;           // 완전한 원형
    object-fit: cover;            // 비율 유지하며 채우기
    border: 3px solid #E50914;    // 넷플릭스 레드 포인트
    margin-bottom: 1rem;          // 아래 여백
}
*/


// ======================================================
// 5️⃣ 기본 링크 스타일
// ======================================================

/*
a {
    text-decoration: none;  // 밑줄 제거
    color: white;           // 글자색
    cursor: pointer;        // 마우스 올리면 손가락 모양
}
*/

// cursor: pointer를 넣으면 클릭 가능하다는 시각적 힌트 제공


// ======================================================
// 6️⃣ Grid 레이아웃 (링크 리스트)
// ======================================================

/*
.link-list ul {
    display: grid;                 // Grid 선언
    grid-template-columns: 1fr;    // 기본 1열 (모바일)
    gap: 1rem;                     // 요소 사이 간격
    list-style: none;              // 점 제거
    max-width: 600px;              // 최대 너비 제한
    margin: 0 auto;                // 가운데 정렬
    padding: 0 20px;               // 좌우 여백
}
*/

// 1fr = 남은 공간을 1 비율로 채움


// ======================================================
// 7️⃣ 카드 디자인 (Flex 내부 정렬)
// ======================================================

/*
.link-list li a {
    display: flex;             // 내부 요소 정렬
    align-items: center;       // 세로 가운데
    justify-content: center;   // 가로 가운데

    background-color: #333;    // 카드 배경색
    padding: 1.2rem;           // 내부 여백
    border-radius: 8px;        // 둥근 모서리
    font-weight: bold;         // 굵은 글씨
    border: 1px solid transparent; // 호버 대비용
}
*/


// ======================================================
// 8️⃣ Hover 인터랙션 효과
// ======================================================

/*
.link-list li a {
    transition: all 0.3s ease;  // 0.3초 동안 부드럽게 변화
}

.link-list li a:hover {
    background-color: #E50914;  // 레드 강조
    transform: scale(1.05);     // 1.05배 확대
    box-shadow: 0 10px 20px rgba(229, 9, 20, 0.4); // 그림자 효과
}
*/

// transition이 없으면 갑자기 바뀌어 부자연스러움


// ======================================================
// 9️⃣ Responsive (Media Query)
// ======================================================

/*
@media (min-width: 768px) {
    .link-list ul {
        grid-template-columns: 1fr 1fr; // 2열 배치
    }

    .link-list li:first-child {
        grid-column: span 2; // 첫 번째 카드 2칸 차지
    }
}
*/

// min-width: 768px
// → 화면이 768px 이상일 때만 아래 코드 적용


// ======================================================
// 🔟 핵심 개념 요약
// ======================================================

// Flexbox  → 1차원 정렬 (한 줄 정렬에 강함)
// Grid     → 2차원 정렬 (카드 배치에 강함)
// Hover    → 사용자 인터랙션
// Media Query → 반응형 웹 구현

// CSS는 디자인이 아니라 "레이아웃 설계 기술"이다.

